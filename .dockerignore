# 빌드 컨텍스트에서 제외 — 이미지 크기/보안/재빌드 속도
.git
.github
.venv
__pycache__
*.pyc
*.pyo
.pytest_cache
.ruff_cache
.coverage
htmlcov
dist
build

# 데이터 + 리포트 (bind mount 대상)
data/cache
data/reports
tests/cassettes
notebooks

# 프런트는 별도 컨텍스트로 빌드
web

# 환경파일은 compose env_file/environment로 주입
.env
.env.local

# IDE
.vscode
.idea

# 스크린샷 등
*.png
*.jpg
